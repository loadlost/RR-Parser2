# RR-Parser

Проект предназначен для автоматической обработки кадастровых номеров с помощью данных Росреестра.
Он отправляет запросы, обрабатывает полученные данные и сохраняет результаты в Excel файл.

## Начало работы

Для работы с проектом клонируйте репозиторий на свой локальный компьютер, установите необходимые зависимости,
указанные в файле `requirements.txt`, и выполните настройку конфигурационного файла.

### Предварительные условия

Убедитесь, что у вас установлен Python 3 и все необходимые библиотеки:

--logging: библиотека для логирования.
--time: библиотека для работы со временем.
--openpyxl: библиотека для чтения/записи файлов Excel 2010 xlsx/xlsm/xltx/xltm.
--pandas: библиотека для анализа данных, предоставляющая структуры данных и функции для работы с табличными данными.
--requests: библиотека для отправки HTTP запросов.
--python3_anticaptcha: Библиотека для обхода CAPTCHA с использованием сервисов AntiCaptcha.
--datetime: библиотека для работы с датами и временем.


## Конфигурация

Перед началом использования проекта необходимо настроить следующие параметры:

- **Прокси**: В файле `config.py` укажите данные вашего прокси:
	proxy_url = "http://login:password@ip_address:port"

- **API ANTICAPTCHA**: Укажите ваш ключ для API ANTICAPTCHA:
	ANTICAPTCHA_KEY = "Ваш_Ключ_API"

- **Кадастровые номера**: добавьте кадастровые номера в список cad_numbers:
	cad_numbers = [
    '77:06:0009004:1020',
    // Добавьте свои номера здесь
	]